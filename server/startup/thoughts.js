// Startup script for thoughts collection

Meteor.startup(function() {
    /*
     You can seed data or set anything you want to be executed once on app startup,
     which is executed only when the app is built and run for first time, not for every http call
     */
});